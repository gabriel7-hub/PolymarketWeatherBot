requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.26
scipy>=1.11
pandas>=2.1
# Execution (only needed when you enable live trading). v2 is required for
# POLY_1271 (signature_type=3) smart-contract / pUSD wallets.
py-clob-client-v2>=1.0.1
flask>=3.0
